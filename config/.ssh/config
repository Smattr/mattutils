# This file contains public settings. For private SSH configs, see my private
# or NICTA repos.

# Toshiba netbook? Can't remember which machine this is.
Host stochasm
    User matthew

# Re-use TCP connections for new SSH sessions.
Host *
    ControlMaster auto
    ControlPath ~/.ssh/master-%r@%h:%p

# Turn on some nicer settings for external hosts. Not really required for
# internal, but it doesn't hurt.
Host *
    TCPKeepAlive yes
    Compression yes
