#compdef no

# Zsh completion for no

_arguments \
  '--allow-network[allow network access]' \
  '--allow-tmp-writes[allow writes to TMPDIR]' \
  '--allow-writes[allow file system writes]' \
  '--disallow-network[disallow network access]' \
  '--disallow-writes[disallow file system writes]' \
  '--help[show man page]'
